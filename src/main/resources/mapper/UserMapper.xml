<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.security.security.mapper.UserMapper">

    <insert id="signup_user" parameterType="UserVO">
      INSERT INTO user
             (user_email, user_pw, user_name, user_phone, user_create_date)
      VALUES
            (#{user_email}, #{user_pw}, #{user_name}, #{user_phone}, NOW())
    </insert>
    
    <select id="check_user_id" parameterType="String" resultType="UserVO">
      SELECT *
      FROM user
      WHERE user_email = #{user_email}
    </select>
</mapper>